# Ticket Tracker — Frontend

React + TypeScript client for the internal ticket tracking app.

## Stack

- React + TypeScript
- Vite
- TanStack Query (data fetching, caching, mutations)

## Setup

```bash
cd frontend
npm install
```

## Run

```bash
npm run dev
```

The app will be available at `http://localhost:5173`.
